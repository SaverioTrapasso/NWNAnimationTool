# Godot 4+ specific ignores
.godot/
/android/

# Godot editor/runtime executables (too large for GitHub, download separately)
/SDK/

# Local tooling/output, not part of the project source
/.claude/
/export/
/build/
/anim_GLB/
/anim_ASCII/

# Claude Code project instructions — local tooling, stays private
/CLAUDE.md

# Personal working material (pose reference images/videos), never publish
/emotegui/
/TEMP_emotegui/
/Pose_Test.png

# Auto-downloaded MediaPipe model (detect_pose.py fetches it on first run)
ai_pose/pose_landmarker.task

# Build outputs
/build/
/Pose_Test.png.import
